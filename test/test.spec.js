'use strict'
import {expect} from 'chai';
let message = require('../app/Components/Message/message.jsx');


describe('Test', () => {

    it("test", (done) => {
        expect(0).to.equal(1);
    });
        
})